# 2b2t Market Discord Bot

## updates

### Added:

 added the commands: `/ping` `/echo` `/update` `/restart`. <br>
 added the bot version.

### removed:

none 

Version: v0.0.1.1 - Pre 1 - test build 5

## commands

`/ping` test's if the bot respons. returns `Pong [ms delay]`. <br>
`/echo` echo's back what you saided. <br>
`/update` pulles the latest repo from github and resarts the bot. <br>
`/restart` restarts the bot 

## developers

 * Kitywiel
